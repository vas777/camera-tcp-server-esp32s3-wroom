[package]
name = "relay"
version = "0.1.0"
edition = "2024"

[dependencies]
log = "0.4.32"
shared = { path = "../shared", features = ["std"] }
simple_logger = "5.2.0"
tokio = { version = "1.52.3", features = ["full", "net", "rt"] }
